services:
  app:
    build:
      target: runner
    volumes:
      - ./src:/app/src
      - ./package.json:/app/package.json
      - /app/node_modules             # <-- ADICIONE ESTA LINHA EXATAMENTE ASSIM
    command: ["bun", "--hot", "run", "src/index.ts"]
